spring.config.import=optional:file:./secrets.properties

spring.application.name=usuarios
server.port=8081

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:milsaboresdb}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:1234}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#es para saber si me estoy conectando a neon o a la db local
logging.level.com.zaxxer.hikari=DEBUG

#tokens JWT
app.jwt.secret=una-clave-muy-larga-y-secreta-por-favor-c�mbiala-por-una-segura-256bits-min
app.jwt.expiration-ms=86400000

# CORS con el puerto de vercel
app.frontend.origins=http://localhost:5173,http://localhost:3000
